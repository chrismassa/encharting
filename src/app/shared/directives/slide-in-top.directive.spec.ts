import { SlideInTopDirective } from './slide-in.directive';

describe('SlideInTopDirective', () => {
  it('should create an instance', () => {
    const directive = new SlideInTopDirective();
    expect(directive).toBeTruthy();
  });
});
